#ifndef BLYNK_HANDLERS_H
#define BLYNK_HANDLERS_H

// No functions to declare here — BLYNK_CONNECTED() and BLYNK_WRITE(V12)
// self-register with the Blynk library as soon as blynk_handlers.cpp
// is compiled. This header just needs to exist so the .ino can #include
// it and force that .cpp into the build.

#endif
